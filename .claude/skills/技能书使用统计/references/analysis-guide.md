# 数据分析指南

> 本文档提供技能书使用统计的分析方法和可视化技巧。

---

## 分析维度

### 1. 使用频率分析

**目的**：识别高频、中频、低频和未使用的技能书

**方法**：
```bash
# 按使用次数排序
cat stats.json | jq -r '.skills | to_entries | sort_by(.value.usageCount) | reverse | .[] | "\(.value.name): \(.value.usageCount)次"'
```

**输出示例**：
```
Git提交规范: 15次
Java微服务开发: 8次
全栈开发规范: 5次
前端设计: 2次
技术文档编写: 1次
文档编辑规范: 0次
项目规范: 0次
```

**分类标准**：
- **高频技能**（>10次）：核心技能，使用频繁
- **中频技能**（5-10次）：常用技能，使用稳定
- **低频技能**（1-4次）：偶尔使用，需关注
- **未使用技能**（0次）：从未使用，需分析原因

---

### 2. 时间分布分析

**目的**：了解技能书的使用时间模式

**按日统计**：
```bash
# 按日期分组统计
cat stats.json | jq -r '[.skills[].records[]] | group_by(.timestamp[0:10]) | .[] | "\(.[0].timestamp[0:10]): \(length)次"'
```

**输出示例**：
```
2026-02-26: 9次
2026-02-27: 12次
2026-02-28: 6次
```

**按周统计**：
```bash
# 按周统计（需要额外脚本）
cat stats.json | jq -r '[.skills[].records[]] | .[].timestamp[0:10]' | \
  xargs -I {} date -j -f "%Y-%m-%d" {} "+%Y-W%W" | \
  sort | uniq -c
```

**按月统计**：
```bash
# 按月统计
cat stats.json | jq -r '[.skills[].records[]] | group_by(.timestamp[0:7]) | .[] | "\(.[0].timestamp[0:7]): \(length)次"'
```

---

### 3. 使用方式分析

**目的**：了解技能书是自动触发还是手动调用

**统计方法**：
```bash
# 统计各使用方式的次数
cat stats.json | jq -r '[.skills[].records[]] | group_by(.method) | .[] | "\(.[0].method): \(length)次"'
```

**输出示例**：
```
auto: 15次 (55.6%)
manual: 10次 (37.0%)
reference: 2次 (7.4%)
```

**分析**：
- **auto 占比高**：说明技能书已融入工作流，自动化程度高
- **manual 占比高**：说明需要用户主动调用，可能需要优化触发机制
- **reference 占比高**：说明技能书主要作为参考，实际执行较少

---

### 4. 执行结果分析

**目的**：评估技能书的遵循情况和成功率

**统计方法**：
```bash
# 统计各执行结果的次数
cat stats.json | jq -r '[.skills[].records[]] | group_by(.result) | .[] | "\(.[0].result): \(length)次"'
```

**输出示例**：
```
success: 20次 (74.1%)
partial: 5次 (18.5%)
failed: 2次 (7.4%)
```

**计算成功率**：
```bash
# 计算成功率
cat stats.json | jq '[.skills[].records[]] | (map(select(.result == "success")) | length) / length * 100'
```

**分析**：
- **成功率 >80%**：优秀，技能书规范明确且易于执行
- **成功率 60-80%**：良好，但需要改进部分规范
- **成功率 <60%**：需要重点优化，规范可能不合理或过于复杂

---

### 5. 场景覆盖分析

**目的**：识别应该使用但未使用的场景

**场景定义**：

| 场景 | 应该使用的技能 | 检测方法 |
|------|---------------|----------|
| 提交代码 | Git提交规范 | 检查是否有 git commit |
| 开发后端 API | Java微服务开发 | 检查是否创建了 Controller |
| 前后端联调 | 全栈开发规范 | 检查是否同时修改了前后端 |
| 创建前端页面 | 前端设计 | 检查是否创建了 .vue 文件 |
| 编写文档 | 技术文档编写 | 检查是否创建了 .md 文件 |
| 修改数据库 | 文档编辑规范 | 检查是否有 .sql 文件 |
| 设计接口 | 项目规范 | 检查是否查阅了响应格式 |

**分析脚本示例**：
```bash
#!/bin/bash

# 检查 Git 提交记录
git log --oneline --since="1 week ago" | wc -l
# 如果有提交，检查是否有对应的技能书记录

# 检查 Controller 文件变更
git diff --name-only HEAD~10 | grep "Controller.java" | wc -l
# 如果有 Controller 变更，检查是否使用了 Java 微服务规范

# 检查前端文件变更
git diff --name-only HEAD~10 | grep ".vue" | wc -l
# 如果有 .vue 文件变更，检查是否使用了前端设计规范
```

---

### 6. 技能关联分析

**目的**：了解哪些技能经常一起使用

**分析方法**：
```bash
# 查找同一时间段内（10分钟内）使用的技能
cat stats.json | jq -r '
  [.skills[] | .records[] | {skill: .skill, timestamp: .timestamp}] |
  group_by(.timestamp[0:15]) |
  map(select(length > 1)) |
  .[] | map(.skill) | @csv
'
```

**常见组合**：
- Git提交规范 + Java微服务开发（后端开发流程）
- 全栈开发规范 + 前端设计（全栈功能开发）
- 项目规范 + Java微服务开发（新接口设计）
- 文档编辑规范 + Java微服务开发（数据库变更）

---

## 可视化方法

### 1. 文本图表

**使用排行榜**：
```bash
#!/bin/bash

cat stats.json | jq -r '.skills | to_entries | sort_by(.value.usageCount) | reverse | .[]' | while read -r skill; do
  name=$(echo "$skill" | jq -r '.value.name')
  count=$(echo "$skill" | jq -r '.value.usageCount')
  total=$(cat stats.json | jq '.summary.totalUsage')

  # 计算百分比
  percent=$(echo "scale=1; $count * 100 / $total" | bc)

  # 生成条形图
  bars=$(printf '█%.0s' $(seq 1 $(echo "$count * 2" | bc)))

  echo "$name: $bars $count次 ($percent%)"
done
```

**输出示例**：
```
Git提交规范:      ████████████████████ 15次 (55.6%)
Java微服务开发:    ████████████ 8次 (29.6%)
全栈开发规范:      ████ 5次 (18.5%)
前端设计:          ██ 2次 (7.4%)
```

### 2. 时间线

**按时间展示使用记录**：
```bash
cat stats.json | jq -r '[.skills[] | .records[]] | sort_by(.timestamp) | .[] | "\(.timestamp[0:16]) [\(.result)] \(.details[0:50])..."'
```

**输出示例**：
```
2026-02-26 14:28 [success] 使用约定式提交格式创建 commit...
2026-02-26 15:30 [success] 创建 UserController，实现 CRUD 接口...
2026-02-26 16:00 [success] 后端创建 /api/users 接口，返回 RI<PageResult...
2026-02-26 17:00 [success] 使用 Vue 3 + Element Plus 创建登录页面...
```

### 3. 日历热力图（文本版）

```bash
#!/bin/bash

# 按日期统计使用次数
for day in $(seq -7 0); do
  date=$(date -v-${day}d "+%Y-%m-%d")
  count=$(cat stats.json | jq "[.skills[].records[] | select(.timestamp | startswith(\"$date\"))] | length")

  # 根据次数显示不同符号
  if [ $count -eq 0 ]; then symbol="·"
  elif [ $count -le 3 ]; then symbol="▪"
  elif [ $count -le 6 ]; then symbol="▪▪"
  else symbol="▪▪▪"
  fi

  echo "$date: $symbol ($count次)"
done
```

**输出示例**：
```
2026-02-20: ▪ (2次)
2026-02-21: ▪▪ (5次)
2026-02-22: · (0次)
2026-02-23: ▪▪▪ (9次)
2026-02-24: ▪▪ (4次)
2026-02-25: ▪ (3次)
2026-02-26: ▪▪▪ (10次)
```

---

## 报告模板

### 每周统计报告

```markdown
# 技能书使用周报（2026-W08）

## 概览
- 总使用次数: 27次（比上周 +5次）
- 活跃技能数: 5/7 (71.4%)
- 成功率: 81.5%
- 覆盖率: 71.4%

## 使用排行
1. Git提交规范 - 15次 ⬆️
2. Java微服务开发 - 8次 ⬇️
3. 全栈开发规范 - 5次 ➡️
4. 前端设计 - 2次 🆕
5. 技术文档编写 - 1次 🆕

## 本周亮点
✅ 新激活技能：前端设计、技术文档编写
✅ Git提交规范使用次数显著增长
✅ 整体成功率提升至 81.5%

## 需要改进
⚠️ Java微服务开发成功率 75%，部分场景未遵循命名规范
⚠️ 仍有 2 个技能未使用：文档编辑规范、项目规范

## 下周计划
- [ ] 提高 Java 微服务开发规范遵循率
- [ ] 尝试使用文档编辑规范记录数据库变更
- [ ] 使用项目规范进行接口设计评审
```

### 月度分析报告

```markdown
# 技能书使用月报（2026-02）

## 数据摘要
- 总使用次数: 105次
- 活跃技能数: 6/7 (85.7%)
- 平均成功率: 78.1%
- 日均使用: 3.75次

## 使用趋势
📈 增长最快: Git提交规范（+200%）
📉 下降最多: Java微服务开发（-15%）
🆕 新激活: 前端设计、技术文档编写

## 技能分析

### Git提交规范
- 使用次数: 45次
- 成功率: 95.6%
- 评价: ⭐⭐⭐⭐⭐ 优秀
- 建议: 继续保持

### Java微服务开发
- 使用次数: 30次
- 成功率: 73.3%
- 评价: ⭐⭐⭐⭐ 良好
- 建议: 加强命名规范检查

### 全栈开发规范
- 使用次数: 18次
- 成功率: 83.3%
- 评价: ⭐⭐⭐⭐ 良好
- 建议: 继续保持前后端同步

### 前端设计
- 使用次数: 8次
- 成功率: 87.5%
- 评价: ⭐⭐⭐⭐ 良好
- 建议: 增加使用频率

### 技术文档编写
- 使用次数: 4次
- 成功率: 100%
- 评价: ⭐⭐⭐⭐⭐ 优秀
- 建议: 养成同步更新文档习惯

### 文档编辑规范
- 使用次数: 0次
- 评价: ❌ 未使用
- 建议: 下月必须启用，记录数据库变更

### 项目规范
- 使用次数: 0次
- 评价: ❌ 未使用
- 建议: 新接口设计前查阅响应格式规范

## 场景覆盖分析

| 场景 | 应使用技能 | 实际使用 | 覆盖率 |
|------|-----------|---------|--------|
| 提交代码 | Git提交规范 | 45/45 | 100% ✅ |
| 后端开发 | Java微服务开发 | 30/35 | 85.7% ⚠️ |
| 前后端联调 | 全栈开发规范 | 18/20 | 90% ✅ |
| 前端页面 | 前端设计 | 8/12 | 66.7% ⚠️ |
| 编写文档 | 技术文档编写 | 4/15 | 26.7% ❌ |
| 数据库变更 | 文档编辑规范 | 0/5 | 0% ❌ |

## 总结
本月技能书覆盖率达到 85.7%，整体表现良好。Git 提交规范已成为标准流程，成功率高达 95.6%。需要加强文档同步和数据库变更记录习惯。

## 下月目标
1. 技能覆盖率达到 100%
2. 整体成功率提升至 85%
3. 文档编辑规范必须启用
4. 提高技术文档编写使用频率
```

---

## 高级分析

### 1. 成功率趋势分析

**目的**：观察技能书遵循情况是否改善

```bash
# 按周统计成功率
cat stats.json | jq -r '
  [.skills[].records[]] |
  group_by(.timestamp[0:10]) |
  .[] |
  {
    date: .[0].timestamp[0:10],
    total: length,
    success: (map(select(.result == "success")) | length)
  } |
  "\(.date): \(.success)/\(.total) = \((.success/.total*100 | floor))%"
'
```

### 2. 技能学习曲线

**目的**：分析技能书从首次使用到熟练的过程

```bash
# 分析某个技能的成功率变化
cat stats.json | jq -r '
  .skills["java-microservice"].records |
  to_entries |
  .[] |
  "\(.key+1). \(.value.timestamp[0:16]) [\(.value.result)]"
'
```

### 3. 相关性分析

**目的**：发现技能书之间的关联关系

**示例**：
- Git提交规范几乎总是和其他技能一起使用
- 全栈开发规范常与 Java微服务开发和前端设计同时使用
- 文档编辑规范应该与 Java微服务开发关联，但实际未关联

---

## 优化建议生成

### 自动生成建议

```bash
#!/bin/bash

# 读取数据
STATS=$(cat stats.json)

echo "## 技能书使用优化建议"
echo ""

# 1. 识别高频技能
echo "### 表现良好的技能"
echo "$STATS" | jq -r '.skills | to_entries | sort_by(.value.usageCount) | reverse | .[] | select(.value.usageCount > 10)' | while read -r skill; do
  name=$(echo "$skill" | jq -r '.value.name')
  count=$(echo "$skill" | jq -r '.value.usageCount')
  echo "✅ $name - 使用${count}次"
  echo "- 建议：继续保持"
  echo ""
done

# 2. 识别需改进技能
echo "### 需要改进的技能"
echo "$STATS" | jq -r '.skills | to_entries | .[] | select(.value.usageCount > 0 and .value.usageCount < 10)' | while read -r skill; do
  name=$(echo "$skill" | jq -r '.value.name')
  count=$(echo "$skill" | jq -r '.value.usageCount')
  # 计算成功率
  # ... 省略 ...
  echo "⚠️ $name - 使用${count}次，需增加使用频率"
  echo ""
done

# 3. 识别未使用技能
echo "### 未使用的技能"
echo "$STATS" | jq -r '.skills | to_entries | .[] | select(.value.usageCount == 0) | .value.name' | while read -r name; do
  echo "❌ $name"
  # 根据技能名称提供建议
  case "$name" in
    "文档编辑规范")
      echo "- 错过场景：数据库变更未记录"
      echo "- 建议：每次数据库变更后使用此技能"
      ;;
    "项目规范")
      echo "- 错过场景：接口设计未查阅规范"
      echo "- 建议：新接口设计前查阅响应格式"
      ;;
  esac
  echo ""
done
```

---

## 常见问题

### Q1: 如何计算覆盖率？
A: `覆盖率 = (活跃技能数 / 总技能数) × 100%`

### Q2: 如何判断技能书是否有效？
A: 查看成功率和使用频率。成功率高且使用频率高说明技能书有效。

### Q3: 未使用的技能是否应该删除？
A: 不一定。先分析是否存在应该使用但未使用的场景。

### Q4: 如何提高技能书使用率？
A:
1. 明确触发场景
2. 优化触发关键词
3. 在工作流中自动化

---

**最后更新**: 2026-02-26
**版本**: v1.0
