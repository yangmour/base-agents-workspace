# 前端动态路由开发教学指南

> 本指南专为后端开发者设计，从零开始学习 Vue 3 + TypeScript 动态路由实现

---

## 📖 目录

1. [前端基础知识](#前端基础知识)
2. [项目结构讲解](#项目结构讲解)
3. [核心概念详解](#核心概念详解)
4. [动态路由实现步骤](#动态路由实现步骤)
5. [常见问题解答](#常见问题解答)

---

## 一、前端基础知识

### 1.1 Vue 3 是什么？

**简单理解：** Vue 3 是一个用于构建用户界面的 JavaScript 框架。

**类比后端：**
- Vue 组件 ≈ Java 类
- Props 传递 ≈ 方法参数
- Emit 事件 ≈ 回调函数
- Store 状态 ≈ 全局变量/缓存

**核心特性：**
- **响应式数据**：数据变化自动更新页面（类似 Spring 的事件监听）
- **组件化**：页面拆分成可复用的组件（类似 Java 的类）
- **单向数据流**：父组件传数据给子组件（类似方法调用）

### 1.2 TypeScript 是什么？

**简单理解：** TypeScript = JavaScript + 类型系统（就像 Java）

**对比：**
```typescript
// TypeScript（像 Java）
interface User {
  id: number
  username: string
}

const user: User = {
  id: 1,
  username: 'admin'
}
```

```java
// Java
public class User {
    private Long id;
    private String username;
}
```

### 1.3 Vue Router 是什么？

**简单理解：** Vue Router 负责根据 URL 显示不同的页面组件。

**类比后端：**
- Vue Router ≈ Spring MVC 的 @RequestMapping
- 路由路径 ≈ API 路径
- 路由守卫 ≈ Spring Interceptor

**示例：**
```typescript
// 前端路由配置
{
  path: '/user',           // URL 路径
  component: UserPage,     // 显示的组件
  meta: { requiresAuth: true }  // 元信息（类似注解）
}
```

```java
// 后端路由（对比）
@GetMapping("/user")
public User getUser() {
    // ...
}
```

### 1.4 Pinia 是什么？

**简单理解：** Pinia 是 Vue 的状态管理库，用于存储全局数据。

**类比后端：**
- Pinia Store ≈ Spring 的 @Service + Redis 缓存
- State ≈ 实例变量
- Actions ≈ 方法

---

## 二、项目结构讲解

### 2.1 完整目录结构

```
admin-frontend/
├── src/
│   ├── api/              # API 接口封装（类似 Feign Client）
│   │   └── menu.ts       # 菜单 API
│   │
│   ├── assets/           # 静态资源（图片、样式）
│   │   └── styles/
│   │       └── main.scss
│   │
│   ├── components/       # 可复用组件
│   │   ├── common/       # 通用组件（按钮、表格）
│   │   └── business/     # 业务组件
│   │
│   ├── composables/      # 组合式函数（工具函数）
│   │   └── usePermission.ts
│   │
│   ├── directives/       # 自定义指令
│   │   └── permission.ts # v-permission 指令
│   │
│   ├── layout/           # 布局组件
│   │   ├── index.vue     # 主布局
│   │   ├── Sidebar.vue   # 侧边栏
│   │   └── Navbar.vue    # 顶部导航
│   │
│   ├── router/           # 路由配置
│   │   ├── index.ts      # 路由主文件
│   │   ├── routes.ts     # 静态路由
│   │   └── permission.ts # 路由守卫
│   │
│   ├── stores/           # 状态管理（Pinia）
│   │   ├── user.ts       # 用户状态
│   │   └── permission.ts # 权限状态
│   │
│   ├── types/            # TypeScript 类型定义
│   │   ├── api.d.ts      # API 类型
│   │   └── router.d.ts   # 路由类型
│   │
│   ├── utils/            # 工具函数
│   │   ├── request.ts    # HTTP 请求封装
│   │   └── storage.ts    # 本地存储
│   │
│   ├── views/            # 页面组件
│   │   ├── login/        # 登录页
│   │   ├── dashboard/    # 仪表盘
│   │   └── system/       # 系统管理
│   │       ├── user/     # 用户管理
│   │       └── role/     # 角色管理
│   │
│   ├── App.vue           # 根组件
│   └── main.ts           # 入口文件
│
├── public/               # 公共资源
├── .env.development      # 开发环境配置
├── .env.production       # 生产环境配置
├── index.html            # HTML 模板
├── package.json          # 依赖配置
├── tsconfig.json         # TypeScript 配置
└── vite.config.ts        # Vite 配置
```

### 2.2 各目录职责

| 目录 | 职责 | 类比后端 |
|------|------|----------|
| `api/` | 封装后端接口调用 | Feign Client |
| `stores/` | 全局状态管理 | Service + Redis |
| `router/` | 路由配置 | Controller 路由 |
| `views/` | 页面级组件 | Controller 方法返回的视图 |
| `components/` | 可复用组件 | 公共工具类 |
| `utils/` | 工具函数 | Utils 类 |
| `types/` | 类型定义 | DTO/VO/Entity |

---

## 三、核心概念详解

### 3.1 组件 (Component)

**概念：** 组件是 Vue 的基本构建块，每个 `.vue` 文件就是一个组件。

**结构：**
```vue
<template>
  <!-- HTML 模板（视图） -->
  <div>{{ message }}</div>
</template>

<script setup lang="ts">
// TypeScript 逻辑（类似 Java 类）
import { ref } from 'vue'

const message = ref('Hello World')
</script>

<style scoped>
/* CSS 样式（只作用于当前组件） */
div {
  color: blue;
}
</style>
```

**类比后端：**
```java
// 后端 Controller（对比）
@RestController
public class UserController {
    private String message = "Hello World";  // 相当于 ref

    @GetMapping("/user")
    public String getUser() {
        return message;  // 相当于 template 显示
    }
}
```

### 3.2 响应式数据 (Reactivity)

**概念：** 数据变化时，页面自动更新。

**实现：**
```typescript
import { ref, reactive } from 'vue'

// ref：适用于基本类型
const count = ref(0)
count.value++  // 修改需要 .value

// reactive：适用于对象
const user = reactive({
  id: 1,
  name: 'admin'
})
user.name = 'new name'  // 直接修改
```

**类比后端：**
```java
// 类似 Spring 的事件监听
@EventListener
public void onDataChange(DataChangeEvent event) {
    // 数据变化时自动触发
}
```

### 3.3 生命周期 (Lifecycle)

**概念：** 组件从创建到销毁的各个阶段。

**常用钩子：**
```typescript
import { onMounted, onBeforeUnmount } from 'vue'

onMounted(() => {
  // 组件挂载后执行（类似 @PostConstruct）
  console.log('组件已加载')
  fetchData()  // 常用于初始化数据
})

onBeforeUnmount(() => {
  // 组件销毁前执行（类似 @PreDestroy）
  console.log('组件即将销毁')
  cleanup()  // 清理定时器、监听器等
})
```

**类比后端：**
```java
@PostConstruct
public void init() {
    // 类似 onMounted
}

@PreDestroy
public void destroy() {
    // 类似 onBeforeUnmount
}
```

### 3.4 Computed 计算属性

**概念：** 基于其他数据计算得出的值，会自动缓存。

**示例：**
```typescript
import { ref, computed } from 'vue'

const firstName = ref('Zhang')
const lastName = ref('San')

// computed 会缓存结果，只有依赖变化才重新计算
const fullName = computed(() => {
  return `${firstName.value} ${lastName.value}`
})
```

**类比后端：**
```java
// 类似 getter 方法
public String getFullName() {
    return firstName + " " + lastName;
}
```

### 3.5 Watch 监听器

**概念：** 监听数据变化，执行副作用操作。

**示例：**
```typescript
import { ref, watch } from 'vue'

const count = ref(0)

watch(count, (newValue, oldValue) => {
  console.log(`count 从 ${oldValue} 变为 ${newValue}`)
  // 可以执行 API 调用、日志记录等
})
```

**类比后端：**
```java
// 类似数据库触发器或 Spring 事件监听
@EventListener
public void onCountChange(CountChangeEvent event) {
    log.info("count changed: {} -> {}", event.getOld(), event.getNew());
}
```

---

## 四、动态路由实现步骤

### 步骤 1: 登录获取 Token

```typescript
// 1. 用户登录
const login = async (username: string, password: string) => {
  const { data } = await authApi.login({ username, password })

  // 2. 保存 Token 到本地
  localStorage.setItem('token', data.accessToken)

  // 3. 保存用户信息到 Store
  userStore.setUserInfo(data.user)
}
```

### 步骤 2: 获取菜单数据

```typescript
// 调用后端 API
const { data: menuTree } = await menuApi.getMenuTree(userId)

// 返回数据格式：
[
  {
    id: 1000,
    name: 'System',
    path: '/system',
    component: 'Layout',
    meta: { title: '系统管理', icon: 'Setting' },
    children: [
      {
        id: 1001,
        name: 'SystemUser',
        path: '/system/user',
        component: 'system/user/index',
        meta: { title: '用户管理', icon: 'User' }
      }
    ]
  }
]
```

### 步骤 3: 转换为路由配置

```typescript
// 将菜单数据转换为 Vue Router 配置
function formatRoutes(menuData: MenuTreeVO[]): RouteRecordRaw[] {
  return menuData.map(menu => ({
    path: menu.path,
    name: menu.name,
    component: loadComponent(menu.component),  // 动态加载组件
    meta: menu.meta,
    children: menu.children ? formatRoutes(menu.children) : []
  }))
}

// 动态加载组件
function loadComponent(component: string) {
  if (component === 'Layout') {
    return () => import('@/layout/index.vue')
  }
  return () => import(`@/views/${component}.vue`)
}
```

### 步骤 4: 动态添加路由

```typescript
// 使用 router.addRoute() 动态添加
const accessRoutes = formatRoutes(menuTree)

accessRoutes.forEach(route => {
  router.addRoute(route)  // 关键 API
})
```

### 步骤 5: 路由守卫验证

```typescript
// router/permission.ts
router.beforeEach(async (to, from, next) => {
  // 1. 检查是否有 Token
  const token = localStorage.getItem('token')

  if (!token) {
    // 未登录，跳转登录页
    next('/login')
    return
  }

  // 2. 检查是否已加载路由
  if (!permissionStore.routesLoaded) {
    // 加载动态路由
    await permissionStore.generateRoutes()

    // 重新导航（重要！）
    next({ ...to, replace: true })
  } else {
    next()
  }
})
```

---

## 五、常见问题解答

### Q1: ref 和 reactive 有什么区别？

**答：**
- `ref`：用于基本类型（string, number, boolean），访问需要 `.value`
- `reactive`：用于对象，直接访问属性

```typescript
// ref
const count = ref(0)
count.value++  // 需要 .value

// reactive
const user = reactive({ name: 'admin' })
user.name = 'new'  // 直接访问
```

### Q2: setup 是什么？

**答：** `setup` 是 Vue 3 Composition API 的入口函数，相当于 Java 类的构造函数。

```vue
<script setup lang="ts">
// 这里的代码在组件创建时执行（类似构造函数）
import { ref } from 'vue'

const count = ref(0)  // 定义响应式数据
</script>
```

### Q3: 为什么需要 import() 动态导入？

**答：** 为了实现代码分割（Code Splitting），按需加载组件，减少首屏加载时间。

```typescript
// 静态导入：所有组件一次性加载
import UserPage from '@/views/user/index.vue'

// 动态导入：只有访问时才加载
const UserPage = () => import('@/views/user/index.vue')
```

### Q4: TypeScript 类型定义有什么用？

**答：** 提供 IDE 智能提示，编译时检查错误，类似 Java 的类型安全。

```typescript
// 定义类型
interface User {
  id: number
  name: string
}

// IDE 会提示可用属性
const user: User = { id: 1, name: 'admin' }
user.  // 输入 . 后会提示 id 和 name
```

### Q5: router.addRoute() 为什么要在守卫中调用？

**答：** 因为需要先登录获取菜单，才能添加路由。守卫会在每次路由跳转前执行。

```typescript
router.beforeEach(async (to, from, next) => {
  // 第一次访问时，动态添加路由
  if (!routesLoaded) {
    await generateRoutes()  // 获取菜单并添加路由
    next({ ...to, replace: true })  // 重新导航
  } else {
    next()  // 已加载，直接放行
  }
})
```

---

## 六、学习路线图

```
第1课: 项目初始化（今天）
  ├── 理解 Vue 3 基础概念
  ├── 配置项目结构
  └── 安装依赖

第2课: 路由基础
  ├── 静态路由配置
  ├── 路由守卫
  └── 路由元信息

第3课: 状态管理
  ├── Pinia 基础
  ├── User Store
  └── Permission Store

第4课: 动态路由核心
  ├── 获取菜单数据
  ├── 转换路由配置
  ├── 动态添加路由
  └── 测试验证

第5课: 布局和菜单
  ├── Layout 布局组件
  ├── Sidebar 菜单组件
  ├── 递归渲染树形菜单
  └── 菜单高亮

第6课: 权限控制
  ├── v-permission 指令
  ├── 按钮权限
  └── 页面权限

第7课: 联调测试
  ├── 前后端对接
  ├── 多角色测试
  └── 问题排查
```

---

## 七、下一步

现在开始创建项目！我会为你生成完整的代码，并逐个文件讲解。

准备好了吗？ 🚀
