# 项目结构规范

## 1. 总体架构

本项目采用**前后端分离 + 多仓库**架构：

```
~/projects/
├── agents-workspace/           # 工作空间（可选 Git 管理）
│   ├── skills/                 # AI 技能书
│   ├── docs/                   # 共享文档
│   ├── scripts/                # 跨项目脚本
│   └── README.md
│
├── agents-backend/             # 后端仓库（独立 Git）
│   ├── server/                 # 微服务
│   ├── common/                 # 共享模块
│   ├── docs/                   # 后端文档
│   ├── 本地开发/               # 开发环境
│   ├── build.gradle
│   └── .git
│
└── agents-frontend/            # 前端仓库（独立 Git）
    ├── src/                    # 源代码
    ├── public/                 # 静态资源
    ├── docs/                   # 前端文档
    ├── package.json
    └── .git
```

---

## 2. 后端项目结构 (agents-backend)

### 2.1 顶层结构

```
agents-backend/
├── server/                     # 微服务目录
│   ├── auth-center/            # 认证中心
│   ├── im-service/             # IM 服务
│   ├── file-service/           # 文件服务
│   ├── weixin-bot/             # 微信机器人
│   └── gateway/                # API 网关（待开发）
│
├── common/                     # 公共模块
│   ├── base-core/              # 核心工具
│   ├── base-knife4j/           # API 文档
│   ├── base-redis/             # Redis 封装
│   └── base-security/          # 安全模块
│
├── docs/                       # 文档
│   ├── specifications/         # 规范文档
│   ├── api/                    # API 文档
│   ├── architecture/           # 架构文档
│   └── yaml/                   # Nacos 配置
│
├── 本地开发/                   # 本地开发环境
│   ├── docker-compose.yml      # Docker 编排
│   ├── dev.sh                  # 开发脚本
│   └── import-nacos-config.sh  # Nacos 配置导入
│
├── scripts/                    # 脚本
│   ├── build-all.sh            # 构建所有服务
│   ├── deploy.sh               # 部署脚本
│   └── test-all.sh             # 测试脚本
│
├── pom.xml                     # Maven 根配置
├── .flattened-pom.xml          # Maven flatten 插件生成
├── .gitignore
└── README.md
```

### 2.2 微服务内部结构（以 auth-center 为例）

```
auth-center/
├── src/main/
│   ├── java/com/xiwen/authcenter/
│   │   ├── config/             # 配置类
│   │   ├── controller/         # REST 控制器
│   │   ├── service/            # 业务逻辑
│   │   │   └── impl/
│   │   ├── mapper/             # MyBatis Mapper
│   │   ├── entity/             # 实体类
│   │   ├── dto/                # 数据传输对象
│   │   │   ├── request/        # 请求 DTO
│   │   │   └── response/       # 响应 DTO
│   │   ├── exception/          # 异常定义
│   │   ├── util/               # 工具类
│   │   └── AuthCenterApplication.java
│   │
│   └── resources/
│       ├── application.yml     # 应用配置
│       ├── bootstrap.yml       # 启动配置（Nacos）
│       ├── mapper/             # MyBatis XML
│       └── db/migration/       # Flyway 数据库迁移
│
├── src/test/                   # 测试代码
├── docs/                       # 服务专属文档
│   ├── README.md
│   ├── API.md
│   └── database/
│       └── schema.sql
│
├── pom.xml
└── README.md
```

---

## 3. 前端项目结构 (agents-frontend)

### 3.1 顶层结构

```
agents-frontend/
├── src/
│   ├── api/                    # API 接口封装
│   │   ├── auth.ts
│   │   ├── user.ts
│   │   ├── im.ts
│   │   └── request.ts          # Axios 配置
│   │
│   ├── assets/                 # 静态资源
│   │   ├── images/
│   │   ├── icons/
│   │   └── styles/
│   │       ├── variables.scss  # 样式变量
│   │       └── global.scss     # 全局样式
│   │
│   ├── components/             # 公共组件
│   │   ├── common/             # 通用组件
│   │   │   ├── Button/
│   │   │   ├── Dialog/
│   │   │   └── Table/
│   │   └── business/           # 业务组件
│   │
│   ├── composables/            # 组合式函数
│   │   ├── useAuth.ts
│   │   ├── useTable.ts
│   │   └── useDialog.ts
│   │
│   ├── directives/             # 自定义指令
│   │   ├── permission.ts
│   │   └── loading.ts
│   │
│   ├── layouts/                # 布局组件
│   │   ├── DefaultLayout.vue
│   │   ├── EmptyLayout.vue
│   │   └── components/
│   │
│   ├── router/                 # 路由配置
│   │   ├── index.ts
│   │   ├── routes.ts
│   │   └── guards.ts           # 路由守卫
│   │
│   ├── stores/                 # Pinia 状态管理
│   │   ├── auth.ts
│   │   ├── user.ts
│   │   └── app.ts
│   │
│   ├── types/                  # TypeScript 类型定义
│   │   ├── api.d.ts            # API 类型
│   │   ├── common.d.ts         # 通用类型
│   │   └── models/             # 数据模型
│   │
│   ├── utils/                  # 工具函数
│   │   ├── request.ts          # HTTP 请求
│   │   ├── storage.ts          # 本地存储
│   │   ├── date.ts             # 日期处理
│   │   └── validate.ts         # 表单验证
│   │
│   ├── views/                  # 页面组件
│   │   ├── auth/
│   │   │   ├── Login.vue
│   │   │   └── Register.vue
│   │   ├── dashboard/
│   │   │   └── Index.vue
│   │   ├── user/
│   │   │   ├── List.vue
│   │   │   └── Detail.vue
│   │   └── im/
│   │
│   ├── App.vue                 # 根组件
│   ├── main.ts                 # 入口文件
│   └── vite-env.d.ts           # Vite 类型声明
│
├── public/                     # 公共资源
│   ├── favicon.ico
│   └── logo.png
│
├── docs/                       # 文档
│   ├── README.md
│   ├── DEVELOPMENT.md          # 开发指南
│   └── COMPONENTS.md           # 组件文档
│
├── .vscode/                    # VSCode 配置
│   ├── settings.json
│   └── extensions.json
│
├── .env.development            # 开发环境变量
├── .env.production             # 生产环境变量
├── .eslintrc.js                # ESLint 配置
├── .prettierrc.js              # Prettier 配置
├── tsconfig.json               # TypeScript 配置
├── vite.config.ts              # Vite 配置
├── package.json
├── pnpm-lock.yaml
├── .gitignore
└── README.md
```

---

## 4. 工作空间结构 (agents-workspace)

```
agents-workspace/
├── skills/                     # AI 技能书
│   └── fullstack-dev/
│       ├── SKILL.md            # 技能描述
│       ├── paths.json          # 项目路径配置
│       └── references/         # 参考文档
│
├── docs/                       # 共享文档
│   ├── onboarding/             # 新人指南
│   ├── architecture/           # 架构决策记录
│   └── changelog/              # 变更日志
│
├── scripts/                    # 跨项目脚本
│   ├── dev-all.sh              # 启动所有服务
│   ├── sync-config.sh          # 同步配置
│   └── code-generator/         # 代码生成器
│
└── README.md                   # 工作空间说明
```

---

## 5. 命名规范

### 5.1 目录命名
- **后端**：小写字母 + 中划线（kebab-case）
  - ✅ `auth-center`, `im-service`
  - ❌ `authCenter`, `ImService`

- **前端**：小写字母 + 驼峰（根据内容类型）
  - 文件夹：小写驼峰（camelCase）- `components`, `composables`
  - Vue 组件文件夹：大驼峰（PascalCase）- `UserList`, `ChatBox`

### 5.2 文件命名
- **Java**：大驼峰（PascalCase）
  - Controller: `UserController.java`
  - Service: `UserService.java`, `UserServiceImpl.java`
  - Entity: `User.java`

- **Vue/TypeScript**：
  - 组件：大驼峰 - `UserList.vue`
  - TS 文件：小写驼峰 - `userApi.ts`, `useAuth.ts`
  - 类型文件：小写 + `.d.ts` - `api.d.ts`

- **配置文件**：小写 + 中划线
  - `docker-compose.yml`, `vite.config.ts`

### 5.3 Git 仓库命名
- 小写字母 + 中划线
- 明确标识用途
- 示例：`agents-backend`, `agents-frontend`, `agents-workspace`

---

## 6. 模块职责划分

### 6.1 后端微服务职责

| 服务名称 | 端口 | 职责 | 数据库 |
|---------|------|------|--------|
| auth-center | 8081 | 用户认证、授权、JWT | PostgreSQL |
| im-service | 8082 | 即时通讯、WebSocket | PostgreSQL |
| file-service | 8083 | 文件上传、存储、预览 | PostgreSQL |
| weixin-bot | 8084 | 微信机器人接入 | MySQL |
| gateway | 8080 | API 网关、路由、限流 | - |

### 6.2 前端模块职责

| 模块 | 职责 |
|------|------|
| api/ | 所有后端接口调用封装 |
| components/ | 可复用 UI 组件 |
| views/ | 页面级组件，路由对应 |
| stores/ | 全局状态管理 |
| router/ | 路由配置和权限控制 |
| utils/ | 纯函数工具，无副作用 |

---

## 7. 文档管理规范

### 7.1 必需文档
每个项目/服务必须包含：
- ✅ `README.md` - 项目概述、快速开始
- ✅ `CHANGELOG.md` - 版本变更记录
- ✅ `.gitignore` - Git 忽略配置

### 7.2 推荐文档
- 📝 `CONTRIBUTING.md` - 贡献指南
- 📝 `DEVELOPMENT.md` - 开发环境搭建
- 📝 `DEPLOYMENT.md` - 部署指南
- 📝 `API.md` - API 接口文档
- 📝 `ARCHITECTURE.md` - 架构设计文档

### 7.3 文档位置
- **项目级文档**：根目录 `README.md`
- **技术规范**：`docs/specifications/`
- **API 文档**：`docs/api/` 或使用 Swagger
- **数据库文档**：`docs/database/`
- **服务专属文档**：各服务的 `docs/` 目录

---

## 8. 环境配置文件

### 8.1 后端配置
```
server/auth-center/src/main/resources/
├── application.yml              # 通用配置
├── application-dev.yml          # 开发环境
├── application-prod.yml         # 生产环境
└── bootstrap.yml                # Nacos 配置
```

### 8.2 前端配置
```
agents-frontend/
├── .env                         # 所有环境公共配置
├── .env.development             # 开发环境（自动加载）
├── .env.production              # 生产环境（build 时加载）
└── .env.local                   # 本地配置（不提交 Git）
```

**示例：`.env.development`**
```ini
VITE_APP_TITLE=智能助手管理后台
VITE_API_BASE_URL=http://localhost:8080/api
VITE_WS_URL=ws://localhost:8082/ws
VITE_UPLOAD_URL=http://localhost:8083/upload
```

---

## 9. 构建产物管理

### 9.1 后端
```
server/auth-center/
├── build/                      # Gradle 构建目录（忽略）
│   ├── classes/
│   ├── libs/
│   │   └── auth-center-1.0.0.jar
│   └── reports/
└── .gitignore                  # 添加 build/
```

### 9.2 前端
```
agents-frontend/
├── dist/                       # 生产构建产物（忽略）
│   ├── index.html
│   ├── assets/
│   └── ...
├── node_modules/               # 依赖（忽略）
└── .gitignore                  # 添加 dist/ 和 node_modules/
```

---

## 10. 依赖管理

### 10.1 后端 (Maven)
- **根目录 `pom.xml`**：定义公共依赖版本和 BOM
- **子模块 `pom.xml`**：继承父 POM，引入所需依赖
- **版本管理**：使用 `<properties>` 和 `<dependencyManagement>`

### 10.2 前端 (pnpm)
- **使用 pnpm**：节省磁盘、提高安装速度
- **workspace**：如果有多个前端应用，使用 pnpm workspace
- **版本锁定**：提交 `pnpm-lock.yaml`

---

## 11. 最佳实践

### ✅ DO
- 保持目录结构清晰、层级不超过 5 层
- 相似功能的文件放在同一目录
- 使用有意义的命名，避免缩写
- 及时更新文档，代码即文档
- 配置文件不提交敏感信息

### ❌ DON'T
- 不要在项目根目录堆积文件
- 不要创建过深的目录嵌套
- 不要使用中文命名文件/目录
- 不要将构建产物提交到 Git
- 不要在代码中硬编码配置

---

## 12. 迁移指南

### 当前项目 → 新结构

**步骤 1：重命名当前项目**
```bash
cd ~/projects/
mv agents-651a8ccb77 agents-backend
```

**步骤 2：创建前端项目**
```bash
cd ~/projects/
# 使用 create-vue 创建
pnpm create vue@latest agents-frontend
```

**步骤 3：创建工作空间（可选）**
```bash
cd ~/projects/
mkdir agents-workspace
cd agents-workspace
git init
```

**步骤 4：更新路径引用**
- 更新 IDE 项目配置
- 更新脚本中的硬编码路径
- 创建技能书配置

---

## 附录：推荐工具

### IDE
- **后端**：IntelliJ IDEA Ultimate
- **前端**：VSCode + Volar

### 浏览器插件
- Vue DevTools
- React DevTools（如果用 React）
- Postman/Apifox

### 命令行工具
- `tree` - 查看目录结构
- `fd` - 快速查找文件
- `rg` (ripgrep) - 代码搜索

---

> **版本**: v1.0
> **更新日期**: 2026-02-05
> **维护者**: AI Assistant
